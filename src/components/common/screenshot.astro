---
import { Image } from "astro:assets";

interface Props {
  src: ImageMetadata;
  alt: string;
}

const { src, alt } = Astro.props;
---

<figure>
  <Image
    src={src}
    alt={alt}
    class="rounded"
    data-lightbox
    loading="lazy"
    decoding="async"
  />
</figure>
